% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_mixture.R
\name{get_clean_posterior}
\alias{get_clean_posterior}
\title{Extract and Process Clean Posterior Samples from RJMCMC Outputs}
\usage{
get_clean_posterior(outputs, mix_size, n_chain)
}
\arguments{
\item{outputs}{List. The RJMCMC outputs, containing \code{jump} matrices for all chains.}

\item{mix_size}{Integer. The desired mixture size (number of components) to filter the posterior samples.}

\item{n_chain}{Integer. The number of chains in the RJMCMC output.}
}
\value{
A list containing two components:
\itemize{
\item \code{summary_post}: A data frame summarizing the posterior samples with columns:
\code{p} (mixing proportions), \code{mu} (means), \code{sigma} (standard deviations),
\code{order} (ranked means), \code{sample} (sample index), and \code{chain} (chain index).
\item \code{post_traj_sum}: A data frame summarizing the posterior trajectory, including mean
and uncertainty intervals (\code{mean_qi}).
}
}
\description{
This function filters and processes posterior samples from RJMCMC outputs
based on a specified mixture size. It generates clean posterior summaries
and the trajectory of the posterior distribution.
}
\details{
The function filters \code{jump} matrices for samples matching the specified \code{mix_size} (number of components).
It constructs a clean summary of posterior parameters and calculates the posterior trajectory
using a sequence of x-values (e.g., for visualization of the mixture distribution).

Steps:
\enumerate{
\item \strong{Filter Samples}:
\itemize{
\item Selects \code{jump} matrices with \code{mix_size} columns.
\item Extracts parameters: mixing proportions (\code{p}), means (\code{mu}), and standard deviations (\code{sigma}).
}
\item \strong{Generate Posterior Trajectory}:
\itemize{
\item Uses the filtered posterior parameters to calculate the PDF of the mixture distribution over a range of x-values.
\item Aggregates the trajectories across samples using \code{mean_qi()} to summarize uncertainty.
}
}
}
