% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rjmc_full.R
\name{check_settings}
\alias{check_settings}
\title{Check and Set Default Values for RJMC Simulation Settings}
\usage{
check_settings(settings, model)
}
\arguments{
\item{settings}{A list of settings for the RJMC simulation. This list can contain various parameters, including:
\itemize{
\item \code{numberChainRuns}: The number of chains to run (default: 4).
\item \code{numberCores}: The number of CPU cores to use for parallel execution (default: equal to \code{numberChainRuns}).
\item \code{numberTempChains}: The number of temperature chains to use (default: 10).
\item \code{iterations}: The number of iterations to run (default: 20000).
\item \code{burninPosterior}: The burn-in period for the posterior (default: 10000).
\item \code{thin}: The thinning interval for the MCMC chains (default: 100).
\item \code{consoleUpdates}: The frequency of console updates (default: 100).
\item \code{numberFittedPar}: The number of parameters to fit (MUST be specified by the user).
\item \code{onAdaptiveCov}: A logical flag indicating whether to use adaptive covariance (default: TRUE).
\item \code{updatesAdaptiveCov}: The frequency of adaptive covariance updates (default: 100).
\item \code{burninAdaptiveCov}: The burn-in period for adaptive covariance (default: 2000).
\item \code{onAdaptiveTemp}: A logical flag indicating whether to use adaptive temperatures (default: TRUE).
\item \code{updatesAdaptiveTemp}: The frequency of adaptive temperature updates (default: 10).
\item \code{onDebug}: A logical flag indicating whether to enable debug mode (default: FALSE).
\item \code{lowerParBounds}: The lower bounds for the parameters (MUST be specified by the user).
\item \code{upperParBounds}: The upper bounds for the parameters (MUST be specified by the user).
\item \code{covarInitVal}: The initial covariance value (default: 1e-10).
\item \code{covarInitValAdapt}: The initial covariance value for adaptive updates (default: 1e-10).
\item \code{covarMaxVal}: The maximum covariance value (default: 1).
\item \code{runParallel}: A logical flag indicating whether to run the simulation in parallel (default: TRUE).
}}

\item{model}{A list representing the model used in the RJMC simulation. The function checks if specific model-related
attributes are available to fill in missing settings (e.g., \code{lowerParSupport_fitted}, \code{upperParSupport_fitted},
and \code{discrete_length}).}
}
\value{
The updated \code{settings} list, with all required settings specified and missing settings filled with default values.
}
\description{
This function checks whether all required settings for the Reversible Jump Monte Carlo (RJMC) simulation are
specified in the \code{settings} list. If any required settings are missing, it assigns default values and prints
messages to notify the user. If critical settings are missing, it raises an error. This ensures that all settings
are correctly specified before running the simulation.
}
\details{
This function ensures that the \code{settings} list contains all necessary values for a successful RJMC simulation. If any
settings are missing, it assigns reasonable default values and notifies the user. Critical settings, such as
\code{numberFittedPar}, \code{lowerParBounds}, and \code{upperParBounds}, must be specified by the user, and an error is raised
if they are missing.
}
\examples{
# Example usage of check_settings function
settings <- list(
  numberChainRuns = 5,
  numberCores = 5,
  numberFittedPar = 3,
  lowerParBounds = c(0, 0, 0),
  upperParBounds = c(10, 10, 10)
)
settings <- check_settings(settings, model = NULL)
print(settings)

}
